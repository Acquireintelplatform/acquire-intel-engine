import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function LeaseExpiryRadarView() {
  return (
    <>
      <Card>
        <SectionTitle>Lease Expiry Radar (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          AI detection of upcoming lease expiries, break clauses, and closure indicators.
        </p>
      </Card>
    </>
  );
}
