import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function DealFlowView() {
  return (
    <>
      <Card>
        <SectionTitle>Deal Flow (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          Track acquisition opportunities from lead → viewing → offer → legals → completion.
        </p>
      </Card>
    </>
  );
}
