import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function PropertyFeedsView() {
  return (
    <>
      <Card>
        <SectionTitle>Property Feeds (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          Live aggregated feeds from all major portals (Realla, CoStar, Rightmove, EG, NovaLoca).
        </p>
      </Card>
    </>
  );
}
