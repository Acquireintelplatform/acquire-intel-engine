import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function PropertySearchView() {
  return (
    <>
      <Card>
        <SectionTitle>Property Search (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          Unified search across all UK property portals + AI filtering + live market indicators.
        </p>
      </Card>
    </>
  );
}
