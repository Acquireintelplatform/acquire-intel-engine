import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function DistressSignalsView() {
  return (
    <>
      <Card>
        <SectionTitle>Distress Signals (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          Live monitoring of distress indicators: insolvencies, Gazette notices, store closures, and
          credit events.
        </p>
      </Card>
    </>
  );
}
