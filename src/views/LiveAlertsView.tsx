import Card from "../components/Card";
import SectionTitle from "../components/SectionTitle";

export default function LiveAlertsView() {
  return (
    <>
      <Card>
        <SectionTitle>Live Alerts (Coming Soon)</SectionTitle>
        <p style={{ color: "#CFE8F3" }}>
          Real-time push notifications for operator movements, store closures, and property triggers.
        </p>
      </Card>
    </>
  );
}
