# Acquire Intel Engine (Frontend)

Production single-page app for the Acquire Intel platform.

## Live URLs

- **Frontend (Render)**: https://acquire-intel-engine-1.onrender.com  
- **API (Render)**: https://acquire-intel-api.onrender.com

> The frontend talks to the API via `VITE_API_BASE`, and the API allows CORS from the frontend origin.

---

## Quick Start (Local)

### Prereqs
- Node 20.x

### Install & run
```bash
npm ci
npm run dev
